# Debug Notes: CMS Video Bugs

## Bug 1: Doppelte Videos in Medienbibliothek
- Media library has 2 entries (id 150001, 150002) for the same file
- Same filename, same size (12990059), different upload timestamps
- User likely clicked upload twice or the upload triggered twice
- Fix: Add deduplication check before inserting into media_library (check filename + fileSize)

## Bug 2: Fehlende Video-Thumbnails
- Videos show only a Video icon, no thumbnail preview
- Need to generate a thumbnail from the video or show a video preview element

## Bug 3: CMS-Änderungen werden nicht übernommen bei KI-Entwicklung
- DB shows: heroImage still has a value (the uploaded image URL)
- DB shows: heroVideoMp4 has the video URL
- The page key is "entwicklung.kientwicklung" 
- The user deleted heroImage and added heroVideoMp4
- But heroImage still has a value in DB → the delete didn't persist
- OR: The page component doesn't read heroVideoMp4 from CMS

Need to check:
1. The KI-Entwicklung page component (SubPageTemplate) - does it read heroVideoMp4?
2. The bulkUpdate mutation - does it handle empty string deletion correctly?
3. The contentDefinitions for the KI page
